# Optional: enable Telegram alerts from the health watchdog.
# Copy to alert.env (same dir) and fill in. If unset, the watchdog still
# logs + restarts the backend, just without sending a Telegram message.
TG_BOT_TOKEN=
TG_CHAT_ID=
