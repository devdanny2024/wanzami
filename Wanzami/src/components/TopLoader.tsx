import { useEffect, useState } from "react";

type TopLoaderProps = {
  active: boolean;
};

// TopLoader intentionally left as a no-op per request to remove it
export function TopLoader({ active }: TopLoaderProps) {
  return null;
}
